{
    "name": "root",
    "private": true,
    "workspaces": {
        "packages": [
            "packages/*"
        ]
    },
    "scripts": {
        "build": "lerna run build --stream",
        "lint": "lerna run lint --stream --parallel",
        "test": "yarn jest --coverage",
        "start:web": "lerna run start --scope @henry/web "
    },
    "devDependencies": {
        "eslint": "^7.25.0",
        "lerna": "^3.22.1",
        "typescript": "^4.2.4",
        "jest": "^26.6.3"
    }
}